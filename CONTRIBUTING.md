# Contributing

- 不提交 Key、Token、Cookie、原始数据、个人论文材料或未脱敏日志。
- 修改流程规则时，同时更新 `SKILL.md`、相关 `references/` 和 `CHANGELOG.md`。
- 新增方法要求说明适用条件、失败判据和证据边界。
- 运行 `python scripts/validate_workflow.py examples/generic-panel-study` 后再提交。
- 不把一次接口成功、文件存在或草稿生成表述为完整研究结论。
